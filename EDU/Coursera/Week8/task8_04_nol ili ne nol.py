print(
    any(
        map(
            lambda x: int(input()) == 0,
            list(
                range(
                    int(input())
                )
            )
        )
    )
)
